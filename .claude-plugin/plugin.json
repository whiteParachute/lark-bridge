{
  "name": "lark-bridge",
  "version": "0.1.0",
  "description": "Bridge Feishu (飞书) conversations to Claude Code or Codex sessions. Supports per-chat backend switching via /provider, manual session reset (/new), session hold (/hold), and live state inspection (/state). Streaming card output, aria-memory integration, session lifecycle management.",
  "author": {
    "name": "whiteParachute"
  },
  "license": "MIT",
  "keywords": ["feishu", "lark", "bridge", "chat", "im"],
  "postInstall": "bash scripts/install.sh"
}
